# 实测结论

这里放**跑出来的东西**：立得住的结论、还没立住的、测试集的盲区、以及踩过的坑。
怎么装怎么跑看 [`README.md`](README.md)，为什么这么设计看 [`sdk/DESIGN.md`](sdk/DESIGN.md)。

> 数字都来自 `lab/` 的场景集与标定脚本，可复现：`npm run compare-stores` / `scripts/calibrate.ts`。

## 这套东西验证了什么

六道闸各管一类失效。截至目前实测立得住的结论：

| 结论 | 证据强度 |
|---|---|
| **⑥ 精确对应「同词不同指」与「实体塌陷」两类，不是笼统提升精度** | 英文语料 26 条：关掉 ⑥ 漏 **7** 条，**一条不多一条不少**全是这两类（批归一化↔特征归一化、EM 收敛↔梯度收敛、类别编码↔特征归一化、四对人名）。同义改写 10 条、过拟合/欠拟合、准确率/精确率、决策树/集成都没漏 —— 那些 ③④ 挡得住 |
| **⑥ 是唯一的防线，不是第六层保险** | 26 条场景 + DeepSeek 真生成：全闸假命中 **0**、通过 **26/26**；关掉 ⑥ → 假命中 **17**。那 17 条**没有一道闸拦下**（闸=`-`），依据错得五花八门：问过拟合拿到决策树、问 F1 拿到「常见问题」、问 LeCun 拿到 Hinton。也就是说 ③④ 在这个尺度上根本挡不住 |
| **支撑度的算子比阈值更要紧** | 重心会稀释信号，取 max 会被旧答案自己的来源片段顶起来（Vapnik→Breiman 就这么漏的），**top-1** 才对。且标定与实现必须同算子 |
| 回答校验的检索必须用保留实体的原文 | 形态上稳定复现（关掉即失明），但**当前量化不可靠**：两条实体用例期望文档不同，随机撞中一条。此前三次"+2 精确落位"建立在已被推翻的个人成绩场景上，证据链作废 |
| **纯余弦分不开近义反义 —— 用通用句对模型时** | 英文问题对上 margin **−0.2936**：L1/L2（该拦）0.9214 > cross-validation 同义改写（该复用）0.6278。任何单一阈值必然同时犯两种错。**限定词是必要的**：换成为语义缓存微调过的编码器，1000 对 QQP 上「正命中率 ≥97% 时的命中率」从 11.6% 升到 60.0%，见下 |
| **主流的出厂默认在精度上不可用** | GPTCache 默认 `similarity_threshold=0.8`（源码 config.py），四份公开数据上正命中率 **49.1%~82.9%** —— paws 上每两次命中就有一次返回错答案，而 49.1% 几乎等于全放行（正例率 48.9%）。同一个 0.8 在 qqp 上是 72.5%、在 paws 上是 49.1%，这就是「阈值必须在你自己的数据上标」的直接证据 |
| **④ 的价值高度依赖负例类型 —— 这解释了之前所有互相矛盾的结论** | 精度约束正命中率 ≥95% 下，③+④ 相对 ③ 单闸：paws（词全一样意思不同）**12.3%→30.4%，+18 个点**；all 6.0%→8.0%；qqp 73.3%→74.3%；pit2015 **20.0%→18.7%，负收益**。「④ 有用」和「④ 是负收益」两个结论都对，取决于测的是哪类负例 —— 18 对课程语料是精挑的近义反义（像 paws），QQP 全集是自然分布 |
| **③ 的精度上限是模型选择，不是机制边界** | 1000 对 QQP、只换编码器：多语种句对模型 11.6%（正命中率≥97% 时的命中率）→ `redis/langcache-embed-v1` **60.0%**（≥95% 时 13.8% → 89.0%）。GPTCache 公布的「命中率 68.8% / 正命中率 >97%」因此是可信的，只是要一个微调过的编码器。**但这只对英文成立**，中文两个模型都分不开 |
| **任务错配会静默失效** | 两次实测：句对模型做检索，「什么是过拟合」top-1 是批归一化（0.366→换检索模型后 0.888）；段落重排器比问题↔问题，中文跨度 0.0013 |
| 资料版本比对有效 | 语料改版两条稳定被 ⑤ 拦下 |

| **「真生成 vs 拼接」是主因，「哪家模型」是次因** | 同一套方法（多采样取中位数）测三个生成端：margin 0.0772（stub）→ 0.0672（claude）/ 0.0626（deepseek）。**两个真生成端彼此的差，小于它们与 stub 的差。** 若成立，⑥ 的阈值不必按模型厂商分别标定 —— 但只有 3 条「该复用」用例，样本太小，当假设看 |
| **stub 下的低通过率是**编码器**的锅，不是生成端** | 26 条场景，只动一个变量：`MODE=stub`+stub 生成 **12/26** → `MODE=local`+stub 生成 **26/26** → `MODE=local`+DeepSeek 真生成 **26/26**。**真编码器 + 拼接式生成就已经满分**，换真 LLM 没有额外贡献。此前说「通过率低是生成端的锅」是错的 —— 那两组对比同时动了编码器和生成端两个变量 |
| **样本太少会让「可分」变成「重叠」** | 标定集从 10 条（该复用 **3**）扩到 42 条（该复用 **19**）后：中位 margin 从 0.0626 涨到 0.1062，但**尾部几乎贴上** —— 最坏情况 0.9547 vs 0.9495，只差 0.0052；同一份 42 条跑两次，一次重叠一次勉强分开。3 条用例时这个风险完全看不见 |
| **随机生成端下，单次标定测的是噪声** | DeepSeek 连跑五轮同一份标定：margin 在 **0.0041 ~ 0.0368** 之间摆，差近 9 倍；建议 θa高 从 0.962 摆到 0.971。原因有两层：生成本身有采样噪声，而脚本用的 `min(该复用)` / `max(该拦下)` 是**极值统计量**，对单个坏样本最敏感。改成每条采样 N 次取中位数后数字才稳 |

| **④ 换对模型是能用的；现在这个不是「效果差一点」而是全盲** | 18 对真实语料对子（中文，9 同义 + 9 近义反义）：`ms-marco-L6` 两组中位都是 0.998，**margin −0.0003**；`bge-reranker-base` **+0.7801**；`mDeBERTa-v3-xnli` +0.2582。所以「④ 是负收益」那条结论前面的限定词是必要的 —— 换模型 ④ 真的能用 |
| **④ 的「形态」和「模型」一样是任务错配的来源** | 同 18 对、同一个 `bge-reranker-base`，只换 candidate 传什么：留一交叉验证 **27.8%（问↔答）对 50%（问↔问）**，训练误差 22% 对 33%。假负从 1 降到 0，直接解掉「④ 开着砍掉 2 次合法复用」那条负收益的成因。反向也成立：NLI 从问↔问换到问↔答直接塌掉（假负 5/9）。所以 `target` 和 θq 捆在一个对象里 |
| **形态的比较立得住，θq 的绝对值立不住** | 同一份 18 对：形态是**配对比较**（同批对子、同模型、只换 candidate），留一下差距比训练误差更大；而 θq 的位置这份数据根本定不出来 —— 错误最少对应的 θ 平台宽 0.62，bootstrap 95% 区间 0.287~0.999。表里那个 0.3494 是平台端点 + 代价不对称，不是测量结果 |
| **「中位好看、尾部重叠」与语言无关** | ④ 在中英两种语言上尾部都重叠，而且**失败的是同样几对**（`accuracy／precision`、`ensemble methods／combining several models`）—— 那是问题对本身难，不是某种语言难。⑥ 的尾部重叠则只在**真生成**下出现，两种语言都出现（中文 −0.0047、英文 −0.1106），stub 生成下两种语言都不重叠 |
| **「中文更依赖 ⑥」是**中位**的差别，不是尾部** | 默认重排器在中文上全盲（中位 margin **−0.0003**），在英文上还有点用（**+0.2752**）。所以英文的 ③④ 能挡住一部分，⑥ 只需管 7 条；中文 ③④ 几乎不挡，⑥ 要管 17 条。两种语言的**尾部**一样重叠，跟这个差别无关 |

尚未立住的：

- **13 条场景集测不出 θa 的差别**。三组阈值（0.97/0.96、0.980/0.963、0.963/0.946）
  跑出完全相同的结果。不是阈值不重要，是这个测试集的支撑度分布太两极 ——
  测阈值要用 `scripts/calibrate.ts` 的探针，不要用 bench。
- **④ 精排在本测试集上实测为负收益**。三段话一起才诚实：
  1. 探针层面，重排器把不可分（margin **−0.2936**）变成勉强可分（**+0.0344**）
  2. 端到端，开与关的假命中同为 0，但复用 **3（开）vs 5（关）** —— 零精度收益，砍掉 2 次合法复用
  3. 但测试集有盲区（见下），④ 的潜在价值被系统性低估
  准确的说法：**用一个任务错配的重排器（`ms-marco` 比问题↔问题），④ 是负收益；
  换成句对相似度训练的模型会怎样，没有验证过。**

> **一条已被推翻的旧结论（二）**：先前说「⑥ 的判别力不依赖生成端」，依据是
> stub 与 `claude -p` 的 margin 几乎一样（0.0348 vs 0.0341）。**那两个数都是 n=1**，
> 而后来量到单轮标定的抖动本身就有 0.03 量级 —— 两个噪声值碰巧接近，被我当成了
> 「几乎不变」。现在的做法是每条用例采样 N 次取中位数，结论要等三个生成端都用
> 同一方法重测过才算数。

> **一条已被推翻的旧结论**：先前两次说「课内跨章那两条六道闸没有一道够得着，只能靠 pre-filter」。
> 那是错的 —— 它们不是拦不住，是被 max 算子放过去了。换成 top-1 后 ⑥ 把两条都拦下，首要依据也都对。
> 我把自己的实现缺陷当成了机制的固有边界，这正是"算子错了比阈值错了更隐蔽"的实例。

## 已知的测试集盲区

判据是「答案的首要依据是不是那篇资料」。当两个概念**住在同一篇资料里**时，这个判据看不出错：

- `L1/L2` 两问都合法指向 n8（同一篇讲义讲了两者）
- `precision/recall` 两问都合法指向 n11

所以三条近义反义用例里有两条**无法被当前判据检出**，④ 的价值因此被系统性低估。要修就得把判据下沉到答案文本层面，或者把这类概念拆成独立文档。

## 踩过的坑

1. 用例最初是「怎么重置密码」「James Bond 是真人吗」—— 跟学生和学科资料没关系
2. 搭了六门课做跨课复用 —— 但一个学生只在一门课里，跨课复用不存在
3. 判据最初是「该不该重生成」—— 有干扰缓存后失效，命中另一条正确缓存也是成功
4. 判据改成「期望文档在依据里」—— 太松，把「复用过拟合的答案给问欠拟合的学生」判成通过
5. 三个模型角色共用一个模型 —— 任务错配，静默失效
6. 答案向量与检索片段跨向量空间比较 —— 余弦无意义
7. 换掉打分器却复用旧尺度的阈值 —— 对照实验因此无效
8. 改了 SDK 的 `Generate` 契约（新增必填 `kind`），无类型的验证台没跟上 —— 13 条全 `null`，静默降级不报错（已转 TypeScript 堵死）
9. ⑥ 的支撑度**标定用单篇文档、实现用 top-k 取 max** —— 阈值失效，还让我把实现缺陷写成了机制边界
10. 中带条目的替换**先删后写** —— 生成一失败（LLM 超时、或产物没有资料依据）就净丢一条本来
    还能用的缓存。而 ⑥ 那条「判不了不驱逐」和「无依据不写入」都是为了「一次故障不改变缓存
    状态」，这一处漏了，那条不变式整个是空话。改成先写后删
11. `all()` 的顺序在内存与真库之间**分叉**：真库有 `ORDER BY created_at, id`，内存返回插入顺序。
    条目按时间顺序写入时两者恰好一致，所以一直看不见 —— 直到一致性脚本里出现一条 createdAt
    更早却最后写入的条目。顺序已写进契约
12. 判别力自检用**分数跨度**而不是 margin —— 一个把「完全无关」打得比「同义改写」还高的
    打分器（顺序整个反过来）只要分数摊得开就能过关。SDK 里 `checkReranker` 早就做对了，
    验证台却自己又实现了一套差一档的
13. 四个阈值只有**一份硬编码默认值**，跨语料、跨编码器、跨生成端通用 —— 于是本文件里英文
    那一轮（θq 0.979 / θa 0.923 / 0.910）从干净 checkout 根本跑不出来，只能改源码。
    现在阈值按 (语料 × 编码器 × 生成端) 存在 `lab/Calibrations.ts`，没标定过的组合不假装有

第 5 和第 7 是同一件事的两面：**阈值属于打分器，不属于流水线**。换打分器必须连阈值一起重标。

第 10、11、12 有一个共同点：**它们都不会报错**，而且都是「散文里写对了、代码里做错了」——
DESIGN 里写着「一次故障不改变缓存状态」、契约里写着「取最新那条」、README 里写着「margin
大于 0 才可分」。所以这一轮给 SDK 补了单测（`sdk/test/`，`node:test`，零依赖）：
每一条坑对应一个测试，改错一行就会有东西变红。第 13 条是它们的反面 —— 那不是代码写错，
是**数据放错了地方**：可复现性靠的不是文档里写着数字，而是那个数字在代码里就是按语境取的。

第 9 更进一层：**算子选错比阈值标错更隐蔽**。阈值错了假命中率降不下来，还有征兆；
算子错了会让标定本身失去意义，然后你把自己的实现缺陷当成机制的性质写进结论。

## 验证台实测（`lab/`）

### 26 条场景 × 真生成：⑥ 的贡献是 17 条

| 场景数 | 生成端 | 全闸打开 | 关掉 ⑥ | ⑥ 的贡献 |
|---|---|---|---|---|
| 13 | stub | 假命中 0 · 通过 5/13 | 9 | 9 |
| 13 | DeepSeek | 假命中 1 · 通过 12/13 | 10 | 9 |
| 26 | stub | 假命中 0 · 通过 12/26 | 16 | 16 |
| **26** | **DeepSeek** | **假命中 0 · 通过 26/26** | **17** | **17** |

两条结论：

1. **「通过率低」全是 stub 的锅。** 同一份 26 条场景，stub 生成 12/26，真生成 **26/26** ——
   判定逻辑一行没改、阈值一个没动。此前所有「通过 N/13」衡量的都是拼接式生成的质量，
   不是缓存判定的精度。
2. **关掉 ⑥ 之后 17 条失败全部是 `reuse` 且闸=`-`**，没有一道闸拦下。③④ 放行了它们 ——
   这跟「④ 用任务错配的重排器是负收益」是一致的：问题侧几乎没有判别力，
   精度全压在 ⑥ 上。

耗时 3.0 + 1.3 分钟（去重后 57 + 26 次生成）。

### 中英对比：全闸一样，关掉 ⑥ 差很多

同一套 26 条场景、**同为 `MODE=local` + stub 生成**，只换语料语言：

| | 中文 | 英文 |
|---|---|---|
| 全闸打开 | 通过 **26/26** | 通过 **26/26** |
| 关掉 ⑥ | 假命中 **17** | 假命中 **7** |

全闸打开两种语言**没有差别**。差别只出现在关掉 ⑥ 之后：中文漏 17、英文漏 7 ——
**中文更依赖 ⑥**。

英文漏的 7 条形态整齐，一条不多一条不少落在 ⑥ 该管的两类上（批归一化↔特征归一化、
EM 收敛↔梯度收敛、类别编码↔特征归一化、四对人名）。中文的 17 条范围更宽，
说明中文的 ③④ 放行了更多本该拦下的。原因**待查** —— 可能是句对模型在中文上的
判别力更弱（那和「重排器在中文上饱和到 0.998」是一致的），但没有直接测过。

> 先前这一节写的是「英文连 stub 都能 26/26，中文要换真生成才到」，据此推出
> 「中文检索更弱」。**那是拿 en/`MODE=local` 跟 zh/`MODE=stub` 比出来的** ——
> 同时动了语言和编码器两个变量。同条件重测后全闸打开两边一样，那个解释站不住。

### 生成调用里 91% 是重复输入

一次 26 条场景的 bench 要 832 次生成，但**只有 73 个不同的输入** —— 那 30 条干扰缓存
每个场景都重灌一遍，输入一字不差，最热的一条重复 27 次。

| | 原来 | 去重后 |
|---|---|---|
| 全闸打开 | 832 | **73** |
| 关掉 ⑥ | 726 | **49** |
| 判定结果 | — | **完全一致** |

去重不只是省钱：对照实验要比的是「开/关某道闸」，生成本身应当是固定函数；
同一份输入在 A/B 两个配置下答得不一样，那个差值里就混进了采样噪声。

### 检索每次都重编整份语料

拆一次未命中的 ask：**embedPassage ×27 占 1257ms（52%）**，而生成只占 845ms。
27 个文档向量每次都一样，却每次重算。按 `id + version` 缓存后
**1205ms → 200ms，快 6 倍**，语料改版自动失效。

一次 bench 因此白花约 8.7 分钟，占 19.8 分钟的 44%。**验证台把自己的实现开销
算到了被测对象头上** —— 和「⑥ 的算子标定与实现分叉」是同一类错误。

### 三个生成端，同一套标定方法

每条用例采样 N 次取中位数（stub 是确定性的，1 次即可）。中文语料，
`multilingual-e5-small` 的 passage 空间，10 条标定用例（3 条该复用 + 7 条该拦下）。

| 生成端 | 采样 | 该复用中位 | 该拦下中位 | margin | 最坏情况（该复用最低 vs 该拦下最高） | 建议 θa高 / 低 |
|---|---|---|---|---|---|---|
| `stub` | 1 | 0.9926 | 0.9154 | **0.0772** | 0.9891 vs 0.9543 | 0.973 / 0.935 |
| `claude-cli` | 3 | 0.9768 | 0.9096 | **0.0672** | 0.9732 vs 0.9407 | 0.960 / 0.926 |
| `deepseek` | 5 | 0.9831 | 0.9205 | **0.0626** | 0.9743 vs 0.9464 | 0.967 / 0.936 |

三件事：

1. **真生成让「该复用」整体下移**（0.9926 → 0.977/0.983）。答案被改写、压缩、综合过，
   离原始片段更远 —— 这正是 stub 测不出来的那部分。
2. **判别力有损失但保住了**：margin 降了 13~19%，不是塌掉。⑥ 的算子在真生成下仍然成立。
3. **两个真生成端彼此的差（0.0672 vs 0.0626），小于它们与 stub 的差。**
   如果这一条经得起更大样本，那么阈值要按「真生成 / 拼接」分，而不必按模型厂商分。

**别把第 3 条当结论。** 只有 3 条「该复用」用例，而最坏情况那一列显示两组的间距
只剩 0.028~0.033 —— 中位数的 margin 看着宽裕，尾部并不。要立住这条得把用例扩到
几十条，并对每个生成端跑同样的采样数。

### 实测：换成真生成之后 θa 怎么变

`scripts/calibrate.ts` 走的是同一个生成实现，所以两种生成端可以直接对比
（10 条标定用例，中文语料，e5-small passage 空间）：

| 生成端 | 该复用最低 | 该拦下最高 | margin | 建议 θa高 / θa低 |
|---|---|---|---|---|
| stub（换序换壳） | 0.9891 | 0.9543 | 0.0348 | 0.980 / 0.963 |
| `claude -p`（真生成） | 0.9719 | 0.9378 | **0.0341** | **0.963 / 0.946** |

**结论比"stub 标不准"更具体：⑥ 的判别力几乎没损失（margin 0.0348 → 0.0341，
差 2%），但整个分布下移了约 0.017。** 也就是说算子是对的，阈值必须跟着生成端重标。

代价是现行默认 0.97 / 0.96 在真生成下余量变得很薄：该复用的最低值 0.9719 只比
θa高 高出 0.0019。仍然分得开，但一次改写风格的波动就能把它推进微调带。

**这套阈值属于生成端，就像 θq 属于重排器。** 换生成端不重标，标出来的数就是
别人分布上的产物 —— 和"换打分器必须连阈值一起换"是同一条规矩。

### 这个场景集测不出阈值差别

三组阈值（0.97/0.96、0.980/0.963、0.963/0.946）跑同一份场景集，**结果完全一样**
（假命中 0，通过 5/13）。不是阈值不重要，是 13 条用例的支撑度分布太两极，
落不进这几条线之间。**测阈值要用标定探针，不要用 bench。**

### 英文语料，真模型，13 条场景

模型：句对 `paraphrase-multilingual-MiniLM-L12-v2` · 检索 `multilingual-e5-small` · 重排 `ms-marco-MiniLM-L-6-v2`
阈值：θq 0.979 / θa高 0.923 / θa低 0.910（由 `scripts/calibrate.ts` 在本语料上标定）

| 配置 | 假命中 | 通过 | 复用 |
|---|---|---|---|
| 全闸打开 | **0** | 13/13 | 3 |
| 关掉 ⑥ | **4** | 9/13 | 7 |
| 关掉「⑥ 用保留实体的原文检索」 | 1 | 12/13 | 5 |
| 关掉 ④ 精排 | **0** | 13/13 | **5** |

关掉 ⑥ 多漏的四条正是它该管的两类：同词不同指 ×2、实体塌陷 ×2。
代价是复用次数从 3 涨到 7 —— **精度归零换来命中率减半**。

第四行是有效对照（SDK 已删掉「无精排就拿 θq 卡余弦」那条退化路径）：
**精排零精度收益，却砍掉 2 次合法复用 —— 在这个测试集上是纯负收益。**
与探针结论一致：`ms-marco` 对问题配对任务错配，θq 0.979 把同义改写也一起拒了。

第三行这一轮**量化不可靠**：两条实体用例期望文档不同，匿名化后随机撞中一条。
形态上仍显示失明，但要更多实体用例才能给出稳定差值。

### 判据

**答案的首要依据（`sourceIds[0]`）是不是那篇资料**，不是「有没有复用」。

- 有干扰缓存后，命中另一条**内容正确**的缓存也是成功
- 必须落在首要依据上：只要期望文档出现在 top-k 里就算过，会把
  「复用了过拟合的答案给问欠拟合的学生」判成通过

**已知盲区**：两个概念住在同一篇资料里时（L1/L2 同属一节、precision/recall 同属一节），
这个判据看不出错，④ 的价值因此被系统性低估。

### 验证台自身的边界

- stub 模式的分数是字符 Jaccard 的哈希投影，**别拿它的 bench 数字说事**
- 13 条是手工构造的，不是真实流量分布。能证伪，不能证明生产精度——那要 100–500 条真实标注
- 「生成」是拼接检索到的首个片段并换序换壳，不是真 LLM。⑥ 的支撑度因此天然偏高，
  能读的是**相对差值**，不是绝对阈值
- 默认重排器 `ms-marco` 是**故意留着的坏例子**（中文饱和、任务也不匹配），
  好让自检卡有东西可挡。真跑 ④ 的精度用中文上唯一标定过的组合：
  `CE_MODEL=Xenova/bge-reranker-base CE_TARGET=answer`
- 因此**中文语料下 ④ 默认是关的**：标定表里那一格没有 θq（`ms-marco` 在中文上落在
  0.9975–0.9988，跨度 0.0013，任何阈值都是常数而不是阈值）。硬填一个数会得到一道恒放行的
  假闸 —— 页面上看着在工作，而「④ 值不值」那张卡会永远输出「两边一模一样」，
  把「模型任务错配」误读成「这道闸没用」。要跑 ④ 就换 `CE_MODEL=` **和 `CE_TARGET=`**
  （θq 属于 (模型 × 形态)，两个都算换），用 `_probe_ce6.ts` 量判别力、`scripts/calibrate.ts`
  出建议值，补一行到 `RERANK_CALIBRATIONS`（或显式 `THETA_Q=`，那就由你自己为它负责）
- 阈值来自 `lab/Calibrations.ts` 里 (语料 × 编码器 × 生成端) 那一行；启动日志和页面横幅会
  写明用的是哪一行、有没有借用或被 `THETA_*` 覆盖。**没标定过的组合借最近的一行并说清楚**

## ④ 重排器候选实测（中文，18 对真实语料对子）

判据是**同义 vs 近义反义**，不含「完全无关」—— ④ 在流水线里只见到 ③ 召回回来的候选，
无关那一类 ③ 的召回下限早就挡掉了。用错判据会得出相反的结论（见下）。

| 模型 | 该复用中位 | 该拦下中位 | 中位 margin | 最坏 margin |
|---|---|---|---|---|
| `Xenova/ms-marco-MiniLM-L-6-v2`（默认） | 0.9981 | 0.9983 | **−0.0003** | −0.0823 |
| `Xenova/bge-reranker-base` | 0.9523 | 0.1722 | **+0.7801** | −0.9984 |
| `Xenova/mDeBERTa-v3-base-xnli-...` | 0.4399 | 0.1817 | +0.2582 | −0.8860 |

不存在 ONNX 版本的（HF 对不存在的仓库返回 `Unauthorized`）：
`Xenova/quora-distilroberta-base`、`Xenova/stsb-*`、`Xenova/mmarco-mMiniLMv2-*`、
`Xenova/bge-reranker-v2-m3`、`Xenova/multilingual-MiniLMv2-*`。所以标准的
「Quora 问题对 / STS-B 句对相似度」cross-encoder 在这条技术栈上拿不到，
`bge-reranker-base` 是目前最好的可得替代。

`bge-reranker` 的失败有规律：**措辞完全不重叠的同义改写会被判成不同**
（`集成方法是什么？／为什么把多个模型合起来会更好？` = 0.0001），
**主题高度重叠的反义对会被判成相同**（`准确率是什么？／精确率是什么？` = 0.9986）。
后者是真实场景里学生最容易连着问的一对。

> **两次栽在同一个坑上。** 第一轮我用 3 对手编探针，其中一对是「完全无关」，
> 据此得出「中文上四个模型全部不可分」。换成 18 对真实对子、去掉不该算的负例之后，
> 结论反过来了。这和标定集从 3 条扩到 42 条时发生的事是同一件 ——
> **探针样本太小，结论就是噪声。**

## 尾部重叠：完整的 2×2

「中位好看、尾部重叠」这个形态我一度当成中文特有，并用它解释「中文更依赖 ⑥」。
补齐四个格之后两条都要改。

**⑥ 支撑度（42 条标定用例）**

| 语言 | 生成端 | 中位 margin | 最坏 margin |
|---|---|---|---|
| 中文 | stub | 0.1025 | **+0.0299** 不重叠 |
| 英文 | stub | 0.1466 | **+0.0179** 不重叠 |
| 中文 | 真生成 | 0.1019 | **−0.0047** 重叠 |
| 英文 | 真生成 | 0.0772 | **−0.1106** 重叠 |

**决定因素是生成端，不是语言。** stub 的答案几乎是原文照抄，支撑度天然贴顶；
真 LLM 的答案改写、压缩、综合过，整组下移（英文该复用中位 0.9896 → 0.8619），
两组因此挤到一起。英文真生成甚至比中文更糟。

**④ 重排（18 对真实对子）**

| 模型 | 语言 | 中位 margin | 最坏 margin |
|---|---|---|---|
| `ms-marco`（默认） | 中文 | **−0.0003** 全盲 | −0.0823 |
| `ms-marco`（默认） | 英文 | +0.2752 | −0.9809 |
| `bge-reranker-base` | 中文 | +0.7801 | −0.9984 |
| `bge-reranker-base` | 英文 | +0.8039 | −0.8619 |

**四格全部尾部重叠，而且失败的是同样几对**：
`ensemble methods／combining several models`（该复用却最低）、
`accuracy／precision`（该拦下却最高）。**难的是问题对本身，不是语言。**

语言影响的是**中位**：默认模型在中文上全盲、英文上还有点用 —— 这才是
「中文关掉 ⑥ 漏 17 条、英文只漏 7 条」的原因。

## QQP 实测：③④ 在 1000 对真人问题对上（英文）

18 对手工对子的问题在于我造的（两次栽在样本量上）。QQP 是真人独立写的问题对、带二元
标签，也是 GPTCache 用的同一份数据，所以这里的数字**可以和公布的对照**。
`scripts/fetchQqp.ts` 取数、`scripts/probeRerankQqp.ts` 出曲线。

指标按主流口径，且**不挑最优点** —— 假正（返回错答案）和假负（白花一次生成）的代价
完全不同，该选哪个点是产品决定：

| 打分器 | F1 最优点 | 正命中率≥97% 时的命中率 | ≥95% | ≥90% |
|---|---|---|---|---|
| ③ 多语种句对模型（现在的默认） | 命中 92.2% / 正命中 72.4% | 11.6% | 13.8% | 25.0% |
| **③ `redis/langcache-embed-v1` · CLS** | 命中 96.4% / 正命中 **91.8%** | **60.0%** | **89.0%** | **97.4%** |
| ③ 同上但 pooling 配成 mean | 命中 92.6% / 正命中 91.5% | 54.8% | 76.6% | 94.0% |
| ④ `ms-marco-L6`（默认重排器） | 命中 85.8% / 正命中 70.4% | **做不到** | 做不到 | 做不到 |
| ④ `bge-reranker-base` | 命中 91.6% / 正命中 69.3% | 1.0% | 5.2% | 5.4% |

> 正例率被 `fetchQqp` 均衡到 50%（QQP 原始约 37%），所以**正命中率这一列偏乐观**；
> 命中率与正确拒绝不受正例率影响。`QQP_BALANCE=0` 可取无偏的那份。

**1. ③ 的天花板高得多，之前测到的是模型选择的后果。** `langcache-embed-v1` 是
gte-modernbert-base 在 Redis 的 langcache-sentencepairs 上微调的（Apache-2.0，有 ONNX，
所以这条技术栈能直接跑）。它把「正命中率 ≥97%」这个语义缓存真正需要的工作点从
11.6% 命中率拉到 60.0% —— **五倍**，且已接近 GPTCache 公布的 68.8%。

**2. ④ 在问↔问形态上，两个现成重排器都进不了高精度区。** `ms-marco` 连 90% 正命中率
都做不到；`bge-reranker-base` 到 95% 时只剩 5.2% 命中率。这和中文 18 对上的结论方向一致，
只是英文数据把它量得更清楚。**Redis 微调过的重排器没有发 ONNX**，所以 ④ 目前没有
「换个微调模型」这条路可走 —— 这也是为什么问↔答形态那个改动值得做。

**3. pooling 配错的代价：一成多的命中率，不报错。**`langcache-embed-v1` 的
`1_Pooling/config.json` 明写 `pooling_mode_cls_token: true`，而 lab 先前 `run()` 一律传
`pooling: "mean"`。拿 mean 跑它，≥97% 那档从 60.0% 掉到 54.8%、≥95% 从 89.0% 掉到 76.6%。
现在 pooling 跟着模型走（`POOLING_BY_MODEL` 表 + `PAIR_POOLING`/`RETR_POOLING` 覆盖），
表里没有的模型会**打警告**而不是默默用 mean。

> **又一次栽在小样本上，这次是我在同一份笔记里栽的第三次。** 我先用 5 对探针测
> pooling，得到 CLS margin +0.0272 / mean **−0.0355（不可分）**，据此写下「配错就从可分
> 变不可分」。1000 对上它只是差一档（60.0% → 54.8%），配错的 mean 甚至仍然远好于
> 现在的默认（54.8% vs 11.6%）。**结论要保留（该用 CLS），理由得改**（少一成命中率，
> 不是坏掉）。前两次分别是 3 对探针和 10 条标定集 —— 同一个坑第三次。

**4. 中文没有跟上 —— 而且决定不追。** `langcache-embed-v1` 的基座是英文 ModernBERT。
拿中文探针跑（n=8，只当方向看，`_probe_recallEncodersZh.ts`）：现在的多语种模型
margin −0.2655、它 −0.0075，**都不可分**；而且它把「完全无关」也打到 0.5533，中文分数
压缩在 0.54~1.0 的窄带 —— 那是英文模型处理中文的趋同，不是判别力。

**往后这套跑分只用英文数据集。**理由不是中文不重要，是**这些结论没有一条是语言相关的**：
⑥ 的算子必须是 top-1、④ 的形态决定尺度、三个模型角色不能共用、先写后删、
`calibratedOn` 必填 —— 英文验证了就是验证了。而英文这边有中文没有的两样东西：
**公开对照**（GPTCache/langcache 的工作点，没有对照的数字只能自己跟自己比）和
**微调过的模型**。中文要重标的只有 θ 的绝对值，而那本来就不跨语言/跨模型/跨生成端迁移。
所以「中文 ④ 全盲、⑥ 要替 ③④ 兜 17 条」这个旧结论，现在看多半也是**模型可得性**的产物
（和「纯余弦分不开近义反义」其实是「通用句对模型分不开」是同一件事），而不是中文的性质。

### ④ 以 ③ 的工作点为条件（同一份 1000 对）

上表 ④ 那两行是在**整个** QQP 上评的，而 ④ 在流水线里只见到 ③ 召回回来的候选。
QQP 的负例大多是完全不同的两个问题（`Why are people so obsessed with having a girlfriend`
／`How can a single male have a child`）—— 那一类 ③ 的召回下限早就挡掉了，正是 18 对那轮
特意排除的一类。所以要以 ③ 为条件重评（`_probe_rerankPipelined.ts`，③ 用 langcache-embed-v1）：

| ③ floor | 通过 | 全放行基线正命中率 | ④ bge：≥97% | ≥95% | ≥90% |
|---|---|---|---|---|---|
| 全集 | 1000/1000 | 50.0% | 1.0% | 5.2% | 5.4% |
| 0.60 | 842/1000 | 59.4% | 1.0% | 5.2% | 13.0% |
| 0.70 | 713/1000 | 70.0% | 1.0% | 5.2% | 26.1% |
| 0.80 | 565/1000 | 86.4% | 1.0% | 5.3% | 51.2% |
| 0.85 | 521/1000 | 91.9% | 1.0% | 5.4% | 基线已达标 |

（`ms-marco` 在**每一档** ③ floor 下三个门槛全部「做不到」。）

**1. 高精度区完全不随 ③ 的工作点变化。**≥97% 恒为 1.0%、≥95% 恒在 5.2~5.4% ——
条件化没有救回 ④。换句话说 ④ 拦不住的那批，不是「被容易的负例稀释了」，是真的拦不住。

**2. ≥90% 那一列的「改善」是假象，我差点写进结论。**子集正例率随 ③ floor 一起涨到
91.9%，而**子集正例率就是全放行的正命中率** —— 门槛 90% 低于基线 91.9% 时，任何打分器
靠「什么都放行」就能达标，报出来是漂亮的 100% 命中率。那不是判别力，是门槛失效。
所以 `bestHitAtPrecision` 现在遇到「门槛 ≤ 基线」直接返回 `baseline-already-passes`，
不给数字 —— 这类格子必须看着刺眼，不能长得像成绩。

**3. 顺带给「④ 端到端零精度收益」一个机制解释。**③ floor 提到 0.85 时基线已经 91.9%，
也就是说**③ 换上好编码器之后自己就把 precision 做到了 91.9%**，剩给 ④ 的判别空间很小，
而那点空间恰好是 ④ 也解决不了的难例（≥95% 时只剩 5.4% 命中率）。这比「④ 没用」准确得多：
**是 ③ 做完了容易的部分，而剩下的部分现成重排器都做不了。**

**5. 还没做的：换 PAWS 那种高词汇重叠的负例。**条件化（见上）只是把完全无关的负例筛掉，
更贴近 ④ 处境的做法是直接用对抗性负例 —— PAWS 里 `Shi Le died, Shi Hu seized power` ／
`Shi Hu died, Shi Le seized power`（标签 0，实体互换）就是 lab 里 Hinton/LeCun 那一类的
真人版。`redis/langcache-sentencepairs-v1` 的 `paws` config 有 10,000 对现成的。

## ④ 的形态：candidate 传旧问题还是旧答案（中文，18 对）

`Reranker` 的注释和 DESIGN 一直写着「传旧问题需要句对模型、传旧答案正好是
query→passage」，但 `RerankStage` 上没有这个字段，实现写死传 `entry.matchText`。
也就是说**上面那张候选表测的全是问↔问形态，而唯一可得的 `bge-reranker-base`
恰恰是 query→passage 训练的** —— 一直在用它做它没被训练过的那件事。

补上这一格（`lab/_probe_ce6.ts`，同 18 对，负例口径 9 对：把 L1/L2、精确率/召回率
并回来算，对 ④ 它们是正当负例，只是 ⑥ 的 `expectDoc` 判据检不出）。
主指标换成**最优单阈值下的错误数** —— margin 只看两个极值，一对坏样本就能把它
打成负数，看不出「除了那一对以外都分得开」：

| 模型 × 形态 | 中位 margin | 最坏 margin | 最优 θq | 错误 | 假负 | 假正 |
|---|---|---|---|---|---|---|
| bge · 问↔问 | +0.7801 | −0.9984 | 0.1228 | 6/18 | 1 | 5 |
| **bge · 问↔答** | +0.8544 | −0.4420 | 0.3494 | **4/18** | **0** | 4 |
| NLI(mDeBERTa-xnli) · 问↔问 | +0.2582 | −0.8860 | 0.0609 | 6/18 | 1 | 5 |
| NLI · 问↔答 | −0.0109 | −0.3607 | 0.0790 | 8/18 | 5 | 3 |

> **「错误」这一列是训练误差** —— 阈值是扫遍这同一份 18 对取最优的，所以偏乐观，
> 而「最优 θq」那一列是一个**很宽的平台的端点**，不是测出来的位置。
> 泛化估计与平台宽度见下一小节；这张表能支持的是**形态之间的比较**，不是任一个 θq 的值。

**1. 换形态把假负清零了，这正是 ④ 负收益的成因。** 「④ 开着零精度收益、还砍掉
2 次合法复用」那条结论，砍掉的就是假负。措辞完全不重叠的同义改写
（`集成方法是什么？`／`为什么把多个模型合起来会更好？`）在问↔问下得 **0.0001**，
在问↔答下得 **0.5573** —— 而且最坏 margin 同时从 −0.9984 收到 −0.4420。

**2. 剩下 4 个假正分两类，性质不同。** 跨篇的 `准确率是什么？`／`精确率是什么？`
（0.9968）是真失效；`L1／L2`（0.9993）和 `精确率／召回率`（0.9981）是**同篇** ——
问↔答形态下 candidate 就是同一段文本，必然同分。这是形态的结构性代价，
而且 **⑥ 也够不着**（同一篇 → 版本一致、支撑度也高）。这一族是六道闸都管不了的，
出路是语料切分或把判据下沉到答案文本，也就是「已知的测试集盲区」那一节说的事。

**3. NLI 换成问↔答直接塌掉**（假负 5/9，中位 margin 转负）。短问句蕴含长答案文本，
方向本就不成立 —— 又一个任务错配的干净实例，同样不报错。**所以换形态和换模型
一样是任务错配的来源**，两者都必须连 θq 一起重标；`RerankStage` 因此把 `target`
和 `floor` 捆在同一个对象里。

> **一条待验证的假设：NLI 当低分否决器。** 两个模型的失败模式看着是正交的 ——
> NLI 对反义对极敏感（`过拟合／欠拟合` 0.0124、`L1／L2` 0.0066），恰好是 bge 在两种
> 形态下都判错的一类。拿 bge(问↔答) 主判、NLI(问↔问) 只做低分否决（δ 落在
> 0.015–0.03 这个平台），18 对上错误降到 **2/18**、假负仍是 0，救回的正是那两个反义对。
> **没有实现，当假设看**：δ 的安全窗口只有 0.0124~0.0349（宽 0.0225），n=18 ——
> 和 θa 尾部只差 0.0052 是同一个形态的问题，扩样本随时会翻。不做成 AND 门槛是因为
> NLI 的正例分数摊得太开（0.0349~0.9603），拿它当门槛会砍掉大量合法复用。

### 那个 θq 有多可信：4/18 是训练误差，不是泛化误差

上面那些错误数**是在同一份数据上选阈值选出来的** —— 扫遍 18 对的所有切点取错误最少的那个。
所以它系统性偏乐观。`lab/_probe_ce7.ts` 对两种形态各做了平台宽度、留一交叉验证、
bootstrap 与负例结构敏感度：

| | 问↔答 | 问↔问 |
|---|---|---|
| 训练误差（阈值在同一份数据上选） | 4/18 = 22.2% | 6/18 = 33.3% |
| **留一交叉验证** | **27.8%** | **50.0%（等于抛硬币）** |
| 最优 θ 的平台 | 0.3494 ~ 0.9720（宽 0.62） | 0.1228 ~ 0.9433（宽 0.82） |
| bootstrap θq 95% 区间 | 0.287 ~ 0.999 | 0.000 ~ 0.999 |
| 换负例结构后的错误率 | 13% ~ 25% | 31% ~ 38% |

**1. 这份数据定不出 θq 的位置。** 问↔答形态下，0.35 到 0.97 之间**任意**阈值都给出同样的
4/18 —— 因为分数极端双峰（5 个负例挤在 0.0006~0.1415，12 个样本挤在 0.9651~0.9995），
中间两段是空的，平台横跨那两个空隙。所以表里那个 0.3494 **不是测量结果，是平台的左端点**。
取左端点而不是中点 0.6607 是按**代价不对称**选的：④ 的假正后面还有 ⑤⑥ 接着，
而假负是净损失。这条理由必须写在 `calibratedOn` 里，否则半年后没人知道那个数是怎么来的。

**2. 泛化误差比训练误差高一档，而且 n=18 上连它自己都不稳。** 留一时留出不同的对子，
选出的 θ 在 0.3494 / 0.6607 / 0.9720 三个值之间跳 —— 平台边界由单个样本决定。

**3. 但「形态」这个结论比「θq 的值」稳健得多。** 留一下 27.8% 对 50%，差距比训练误差那组
（22% 对 33%）**更大**。原因是形态之间是**配对比较** —— 同一批对子、同一个模型、只换
candidate 传什么，配对比较不需要定出阈值的绝对位置。所以可以这么读这一节：
「问↔答优于问↔问」立得住，「θq = 0.3494」立不住。

**4. 负例结构一变，结论就摆。** 现在 9 个负例里有 2 对人名（`Hinton／LeCun`、
`Vapnik／Breiman`），占 22% —— 而它们恰好是 bge 判得最好的一类（问↔答下 0.0006、0.0009）。
去掉它们错误率升到 25%；去掉 2 对同篇降到 13%。**真实流量里人名对不会占到这个比例**，
所以真实错误率大概率落在这个区间的高端。

### 这份探针集与真实流量的差距（已知的，不是猜的）

- **人名对占负例 22%**，真实课程问答里远低于此，而它是最容易判的一类
- **全是干净的构造对子**：没有错别字、口语、不完整句、中英混杂（「overfitting 是啥」）
- **缺一整类真实场景里最危险的情况**：同一个问句但隐含上下文不同（「这个作业怎么交」
  指作业一还是作业三）。探针集里一条都没有，所以 θq 对它**零信息**
- **答案是 `compose()` 拼的两三句**，真 LLM 的答案更长更散 —— 而问↔答形态下 candidate
  就是答案，所以这条直接影响 θq
- **前两次扩样本都翻过结论**：3 对手编探针曾得出「四个模型全部不可分」，扩到 18 对反了过来；
  θa 那边标定集从 10 条扩到 42 条后尾部从「可分」变成只差 0.0052。
  **从 18 对扩到 100 对会不会再反一次，无法排除，而且按前两次的记录该预期会**

### 一个不需要标注就能做的检验

平台宽其实可以是好消息：**如果真实数据的分数分布也是双峰、中间有空隙，那么平台内任意
阈值都工作，θq 的精确位置就不重要了。**风险只在于真实数据把空隙填满。

而验证这一点**不需要标注** —— 拿几百条真实问句对跑一遍 bge 问↔答，只看分数直方图：
是不是双峰、空隙在哪、多少样本落在中间带。成本是几分钟推理，比人工标注便宜两个数量级，
且直接回答「这个阈值机制在你的流量上还成不成立」。**标注要花的钱应该花在中间带那些样本上**，
而不是均匀标一批。

## 和主流基线的同尺度对比

**对比的不是产品，是架构选择。**GPTCache 的核心是「一个 embedding 模型 + 一个
`similarity_threshold`」，功能上正好等于 ③ 单独工作；这个库主张的是 ③ 后面再串一道 ④。
所以在同一份数据上算四档，就能回答「多加一道闸值不值」。功能层面的差异见
[`sdk/DESIGN.md`](sdk/DESIGN.md) 的功能对照表。

四份数据（`scripts/scorePairs.ts` 算分存盘，`scripts/compareBaselines.ts` 出表），
**精度约束取正命中率 ≥ 95%** —— 语义缓存的假命中是返回错答案，代价远高于白花一次生成，
所以看的是「在精度达标的前提下命中率最高能到多少」：

| 数据（正例率） | GPTCache 出厂默认 θ=0.8 | albert 调优 | semcache ③ 调优 | langcache-embed 调优 | ③+④ ms-marco | **③+④ bge** |
|---|---|---|---|---|---|---|
| all（48.6%） | 命中 65.5 / **正命中 62.8** | 4.9% | 3.1% | 6.0% | 5.2% | **8.0%** |
| paws（48.9%） | 命中 98.7 / **正命中 49.1** | 12.3% | 7.2% | 10.0% | 6.4% | **30.4%** |
| pit2015（18.8%） | 命中 19.3 / **正命中 82.9** | 6.0% | 16.0% | **20.0%** | 15.3% | 18.7% |
| qqp（36.5%） | 命中 75.7 / **正命中 72.5** | 1.7% | 6.5% | **73.3%** | 65.8% | 74.3% |

（后五列是「正命中率 ≥95% 时的最高命中率」。第二列是 GPTCache 的**出厂配置实测**，
它达不到 95% 精度，所以列的是它实际的三个率。）

**1. 主流的出厂默认在精度上不可用。** GPTCache 默认 `similarity_threshold=0.8`
（源码 `gptcache/config.py`），在这四份上的正命中率是 **49.1% ~ 82.9%** —— paws 上
每两次命中就有一次返回错答案，而 49.1% 几乎等于全放行（正例率 48.9%）。
这不是 GPTCache 实现得差，是**单一阈值配一个通用编码器**这个组合的必然结果，
也正是「阈值必须在你自己的数据上标」那条规矩的直接证据：同一个 0.8，
在 qqp 上是 72.5% 精度、在 paws 上就掉到 49.1%。

**2. ④ 的价值高度依赖负例类型，而这解释了之前所有互相矛盾的结论。**

| 负例长什么样 | ③ 单闸最好 | ③+④ bge | ④ 的贡献 |
|---|---|---|---|
| paws：词全一样、意思不同（实体互换、词序调换） | 12.3% | **30.4%** | **+18 个点，2.5 倍** |
| all：五来源混合 | 6.0% | 8.0% | +2 个点 |
| pit2015：Twitter 短句，话题不同 | 20.0% | 18.7% | **−1.3 个点** |
| qqp：自然问题对，同为问句、主题相邻 | 73.3% | 74.3% | +1 个点 |

**「④ 有用」和「④ 是负收益」两个结论都对，取决于测的是哪类负例。**18 对课程语料是精挑的
近义反义（像 paws），所以 ④ 看起来必要；QQP 全集是自然分布，所以 ④ 看起来白搭。
这也是「⑥ 是唯一防线」那条结论要重读的地方 —— 在 paws 那类分布上，③④ 挡得住的比之前
测到的多得多。

**3. 换编码器和加一道闸不是可替换的两件事。**qqp 上 `langcache-embed` 单闸就做到 73.3%，
④ 只多 1 个点；paws 上它单闸只有 10.0%，④ 能加到 30.4%。所以：
**编码器解决「够不够像」，④ 解决「像但不是同一件事」** —— 前者能被微调大幅改善，
后者不能，因为高词汇重叠的反义对在 bi-encoder 的向量空间里本来就靠得很近。

**4. 用错的 ④ 在每一份数据上都是负收益。**`ms-marco` 串联后全面低于 ③ 单闸
（qqp 73.3% → 65.8%，paws 10.0% → 6.4%）。二维扫描甚至在 qqp 上把它的阈值压到 0.000 ——
**扫描自己发现这道闸帮不上忙，于是把它关掉了**。这和「任务错配的重排器是负收益」完全一致。

**5. F1 在语义缓存上是误导指标。**GPTCache 出厂默认在 paws 上 F1 = 0.656，
比 ③+④ 串联的 0.462 还高 —— 因为 F1 平均了 precision 和 recall，而它是靠
「几乎全放行」换来的召回。**假命中和假未命中的代价不对称，用一个对称的指标去评它本身就错了。**
所以这套表以「精度约束下的最高命中率」为主，F1 只作参考。

### 性能

| 打分器 | ms/对 | 说明 |
|---|---|---|
| `ce-msmarco`（④ 默认） | 14.2 | 一次成对前向，22M |
| `semcache-pair`（③ 默认） | 25.6 | 两句各编码一次，384 维 |
| `gptcache-albert`（GPTCache 默认） | 40.3 | 768 维 |
| **`ce-bge`（④ 可用替代）** | **87.6** | 278M cross-encoder |
| **`langcache-embed`（③ 微调）** | **108.3** | gte-modernbert 149M |

（本机 CPU、ONNX fp32、四份数据平均。pit2015 一律最快 —— Twitter 短句 token 少，
所以 ms/对 也跟数据分布走。）

两笔账：

- **③ 换微调编码器**：精度五倍（qqp 上 6.5% → 73.3%），代价约四倍编码延迟（25.6 → 108.3 ms）。
  但 ③ 的向量**写入时就算好了**，查询时只编当前这一句 —— 所以这笔延迟是每次请求一次。
- **④ 串联**：`bge` 每个**候选** 87.6 ms，`recallLimit=5` 时是 **438 ms**，而且
  **每次未命中也要付**（④ 判负才去生成）。省下的是一次 LLM 生成（秒级）当然划算，
  但它在 pit2015/qqp 那类分布上换来的是 −1.3 ~ +1 个点。**在这两类分布上，④ 付 438 ms 买不到精度。**

结论落到配置上：**④ 该按负例分布开关，而不是当成一道恒开的闸。**如果线上的重复问题多是
措辞不同的同义改写（像 qqp），把钱花在 ③ 的编码器上；如果多是「改一个实体/数字」的近似问题
（像 paws，也就是课程语料里 Hinton/LeCun 那一类），④ 才是不可替代的。

## 取样本身踩的两个坑（跨数据集验证那一轮）

数字还在跑，但取样阶段这两件事已经是结论了，而且都属于「不报错」那一族。

**一、顺序截断取样在多来源合集上只会拿到第一个来源。** `langcache-sentencepairs-v1`
的 `all` config 按 source 分块排列（offset 0~5000+ 全是 `paws`，30000 才到 `qqp`，
60000 是 `sick`），顺序取前 800 行拿到的 800 对**逐条等于 `paws` 那一份** ——
一份自称「多来源混合」的数据其实是另一份的副本。

发现它靠的是**两份数据的指标一模一样**（F1 最优 θ=0.9168、命中率 97.5%、正命中率
51.1%、正确拒绝 7.7%，三个门槛也全同）。如果只跑一份、或者两份恰好差一点，
这个副本会一路混进结论。改成按固定 seed 跨 split 撒页，并把实际拿到的 source 分布
写进数据文件 —— 现在 `all` 是 paws 200 / qqp 300 / apt 31 / stsb 64 / sick 200。

> **同一类错误一小时内犯了两次。**前一次是 `fetchQqp` 不均衡分支：正负分桶后再合并
> 截断，结果只取到正例、正例率反而变 100%。两次都是「顺序取样导致分布偏差」，
> 而两次都不报错 —— 拿到的是一份看着正常、分布错了的数据。

**二、限流返回的是 HTML 而不是 JSON，而错误消息把状态码顶掉了。**
`datasets-server` 限流时返回整页 HTML，先前 `拉取失败 HTTP ${status}：${text.slice(0,200)}`
把 HTML 头塞进消息，看起来像解析失败而不是被限流。连着拉几个 config 就会撞上
（实测 HTTP 429），一次静默失败让 `paws` 停留在旧版本上 —— **而它是靠「文件缺
`sampling` 字段」才暴露的，不是靠报错**。现在带指数退避重试 4 次，并且
**失败就不写文件**（旧文件原样留着，宁可对不上也不要半份数据冒充完整的）。

这两条和「pooling 配错」「⑥ 取 max 算子」是同一族：**输入或配置错了但程序跑完**。
这套东西的每一层都要有一个「说出自己是什么」的字段（`balanced`、`sampling`、
`sources`、`calibratedOn`、`positiveRate`），否则数据和阈值一样，会悄悄换掉语境。

## 待办与未决

- **④ 要不要换默认模型，仍然没定 —— 但可用的那条路已经标定好了。**
  `CE_MODEL=Xenova/bge-reranker-base CE_TARGET=answer` 在中文上留一交叉验证 27.8%、
  假负 0（问↔问那支是 50%），θq=0.3494 已进 `RERANK_CALIBRATIONS` —— 但那是平台端点
  加代价不对称选的，不是测出来的值，见「那个 θq 有多可信」。默认没动（仍是 `ms-marco × 问↔问`），
  因为改默认形态会让英文那行已标定的 θq=0.979 失配，白白关掉一道在英文上确实
  标定过的闸。要动默认，得先把 `en × bge × 问↔答` 也标出来 —— 探针层面 bge 的
  英文 margin 是 +0.7925，值得标。
- **`zh × bge × 问↔答` 那行只对拼接式生成成立。** 问↔答形态下 candidate 是**生成端
  写的答案**，所以 θq 从此跟着生成端走（问↔问形态不存在这个依赖）。探针用的是
  `compose()` 拼的答案，换 `claude-cli`/`deepseek` 必须重标 —— 这和 θa 那条
  「真生成 vs 拼接是主因」是同一件事，只是现在也落到 ④ 上了。
- **`target: "answer"` 下 ④ 对 plan 条目不适用**，SDK 里标 `off` 并保留 ③ 的名次
  （plan 条目的 `answer` 是空串：拿空串打分会把 plan 全拦掉，回落到 `matchText`
  则是尺度混用）。**代价是混合 scope 下 plan 条目的问题侧只由 ③ 把关**。
  真要给 plan 也上一道精排，得先决定「计划的可比文本」是什么。
- **θa 在真生成下尾部会重叠。** 42 条标定用例下，「该复用」最低（`n3` 梯度下降 0.9547）
  和「该拦下」最高（`n5→n6` 过拟合→欠拟合 0.9495）只差 0.0052，跨轮次会翻转。
  这已经不是「标一个数」的问题：要么接受 ⑥ 在这个尺度上分不开最难的近义对、
  靠 ③④ 兜前面（但实测 ③④ 挡不住），要么换支撑度算子。
- **`CALIB_SAMPLES` 对 DeepSeek 空转，脚本现在会自己报出来。** temperature 0.2 + 相同
  prompt，同一轮内多次采样一字不差；加了 `seed: variant` 之后仍然逐位相同（DeepSeek
  不认这个字段）。所以噪声不在「同轮多采样」能消掉的那个维度上 —— 要看抖动只能整
  脚本跑多轮。`calibrate.ts` 现在检测「N 次全部逐位相同」并打出 **⚠ 采样无效**，
  而不是显示一个假的 `x~x` 区间。
- **并发重复条目已缓解但未消除。** `write()` 落库前查一次同问条目，把窗口从秒级缩到
  毫秒级（实测生成错开完成时只留一条，零耗时穿插时仍留两条）。没有加
  `(scope, match_hash)` 唯一索引 —— 三条理由见 [`sdk/DESIGN.md`](sdk/DESIGN.md) 的并发一节，
  其中最硬的一条：它和「先写后删」的替换策略直接冲突。
- **临时脚本调 `bench()` 时容易忘传 `storeOverride`**，一忘就变成拿内存冒充真库。
  `scripts/compareStores.ts` 传对了，所以三种后端的一致性结论成立；但我自己的
  scratchpad 脚本忘过一次，据此说过一句「Redis 端到端跑通」——那一跑其实在内存上。
- **`hashKey` 实测 300 万条随机串 0 碰撞。** 此前「哈希弱所以不能加唯一索引」的说法
  是没测就断言的，站不住；真正的理由是上面那三条。
